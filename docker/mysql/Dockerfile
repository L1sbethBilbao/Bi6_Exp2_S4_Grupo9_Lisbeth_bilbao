# Imagen de MySQL 8 para el backend (usuarios y base alineados con application.properties)
FROM mysql:8.0

ENV MYSQL_DATABASE=mydatabase
ENV MYSQL_USER=myuser
ENV MYSQL_PASSWORD=password

# MYSQL_ROOT_PASSWORD debe definirse al crear el contenedor (ver docker-compose)
